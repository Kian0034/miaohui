# Windows 版依赖（Windows runner 上安装）
# 注意：
#   - Windows PyPI 视频库名是 "av"（不是 PyAV，PyPI 名称大小写敏感）
#   - PySide6 6.6 对应 Qt 6.6，Win/WinArm 都支持
hnswlib>=0.8.0
av>=12.0,<16.0
onnxruntime>=1.17
numpy<2.3
Pillow>=10
cryptography>=42
jieba>=0.42
appdirs>=1.4
# ASR
faster-whisper>=1.0
# OCR（Windows：RapidOCR 纯离线 ONNX，中文高精度）
rapidocr-onnxruntime>=1.3
opencv-python-headless>=4.9
# UI（Windows 用 PySide6，原生控件 + 全局热键）
PySide6>=6.6
# 模型下载
huggingface_hub>=0.23
